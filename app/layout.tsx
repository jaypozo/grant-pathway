import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Grant Pathway',
  description: 'Unlock up to 30 grant and loan opportunities with Grant Pathways, your trusted small business funding resource. Boost growth with tailored funding reports designed to help you secure capital and thrive.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
